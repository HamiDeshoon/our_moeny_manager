import React from 'react';
import { render } from 'react-dom';
import App from '../App';

localStorage.setItem('duospend_auth_user', JSON.stringify({
  username: 'hamid',
  name: 'Hamid',
  partnerId: 'partner_a',
  avatar: '👨‍💼',
}));
